// angular.module('app')
// 	.filter('getPerson', function($parse) {
//   return function(name, height) {
//     return $parse(path).assign(context, value);
//   };


// })


// app.filter('getById', function() {
//   return function(input, id) {
//     var i=0, len=input.length;
//     for (; i<len; i++) {
//       if (+input[i].id == +id) {
//         return input[i];
//       }
//     }
//     return null;
//   }
// });
		// return function(person, people) {
		// 	for (var i = 0; i<people.length; i++) {
		// 		if (people.name[i] == person) {
		// 			results.personObj = people[i]
		// 			return results.personObj
		// 		}
		// 	}
		// }
